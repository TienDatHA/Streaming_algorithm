# Data folder

Place your local CSV files here before running the experiment:

- `data.csv`
- `training.1600000.processed.noemoticon.csv`

These dataset files are intentionally excluded from git history because they exceed GitHub's file-size limit.
